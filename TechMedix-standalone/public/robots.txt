User-agent: *
Allow: /

# Auth-gated dashboard app — never crawl or index these (route group emits
# noindex too, but the Disallow is defense-in-depth so crawlers don't burn
# budget hitting login-walled pages).
Disallow: /dashboard
Disallow: /ar-mode
Disallow: /maintenance

Sitemap: https://blackcatrobotics.com/sitemap.xml
